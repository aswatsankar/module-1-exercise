//Create an arrow function calculate Triangle Area that takes the base and height of a triangle as parametesr and returns its area
const calculateTriangleArea = (B: number, H: number): number => {
    return 0.5 * B * H;
};


const Trianglearea = calculateTriangleArea(5, 10);
console.log(Trianglearea);
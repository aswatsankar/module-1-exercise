//Create a function calculate Square that takes a number as input and returns its square 

function calculateSquare(num: number): number {
    return num * num;
}
const Squarevalue = calculateSquare(7);
console.log(Squarevalue);